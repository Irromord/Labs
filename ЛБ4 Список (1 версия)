//реализация двоичного дерева с использованием связанного списка (linked list)
public class BinarySearchTree {  
  
    //Представляем узел бинарного дерева  
    public static class Node{  
        int data;  
        Node left;  
        Node right;  
  
        public Node(int data){  
            //Назначаем данные новому узлу, устанавливаем левый и правый дочерние элементы равными null   
            this.data = data;  
            this.left = null;  
            this.right = null;  
            }  
        }  
  
    //Представляем корень бинарного дерева  
    public Node root;  
  
    public BinarySearchTree(){  
        root = null;  
    }  
  
    //factorial() будет вычислять факториал заданного числа  
    public int factorial(int num) {  
        int fact = 1;  
        if(num == 0)  
            return 1;  
        else {  
            while(num > 1) {  
                fact = fact * num;  
                num--;  
            }  
            return fact;  
        }  
    }  
  
    //numOfBST() рассчитает общее количество возможных BST (binary search tree), рассчитав числа Каталана
    public int numOfBST(int key) {  
        int catalanNumber = factorial(2 * key)/(factorial(key + 1) * factorial(key));  
        return catalanNumber;  
    }  
  
    public static void main(String[] args) {  
  
        BinarySearchTree bt = new BinarySearchTree();  
  
        //Показать общее количество возможных бинарных деревьев поиска с ключом 5  
        System.out.println("Total number of possible Binary Search Trees with given key: " + bt.numOfBST(5));  
      }  
}  
